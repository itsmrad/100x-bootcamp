#include <iostream>
using namespace std;

int main() {
  int n;
  cin >> n;
  int arr[n];
  for (int i = 0; i < n; i++) {
    int x;
    int sum = 0;
    cin >> x;
    sum = sum + x;
    arr[i] = sum;
  }

  int q;
  cin >> q;

  for (int i = 0; i < q; i++ ) {
    
  }

  for 
  return 0;
}